# 运行中的服务

## 服务状态

Mini Web项目的服务已经在运行，**不需要启动**。在回答问题或提供解决方案时，请遵循以下规则：

- **前端服务**：已在端口 **5173** 运行
  - 可通过 http://localhost:5173 访问
  - 所有前端代码更改会自动热重载

- **后端服务**：已在端口 **8080** 运行
  - API访问地址为 http://localhost:8080
  - 后端代码修改需要手动重启才能生效

## 开发注意事项

- 不需要提供启动服务的指令或步骤
- 假设用户已经有一个正在运行的开发环境
- 编写解决方案时，可以直接引用这些服务地址
- 前端请求应指向 http://localhost:8080 
- 测试API时可以直接使用curl或浏览器访问 http://localhost:8080/api/*

## 测试账户

默认测试账户可用于登录测试：

- 管理员账户:
  - 用户名: `admin`
  - 密码: `admin123`

- 普通用户:
  - 用户名: `user`
  - 密码: `admin123`

## 连接配置示例

测试远程连接时，可以使用以下示例配置：

```json
{
  "name": "本地SSH测试",
  "protocol": "ssh",
  "host": "localhost",
  "port": 22,
  "username": "testuser",
  "authType": "password"
}
```