# Mini-Web 未完成功能修复方案

**创建日期**: 2026-03-20  
**状态**: 规划中

## 任务概述

基于代码库深度分析，项目存在多处违反核心三大原则（不使用模拟/简化/临时方案）的实现，需按优先级逐一修复。

---

## P0 — 违反「不使用模拟方案」原则

### M-01: getUserIDFromContext stub
- **文件**: `mini-web/backend/internal/api/auth_handler.go:212`
- **问题**: 永远返回 userID=1，所有操作日志归因错误
- **修复方案**: 改用 `middleware.GetUserID(r)` 读取真实 JWT 上下文
- **影响范围**: auth_handler.go, system_handler.go, email_handler.go
- **预估工时**: 1小时
- [x] T-01 实施修复 ✅

### M-02: Dashboard 所有统计数据硬编码
- **文件**: `mini-web/backend/internal/service/dashboard_service.go:200-320`
- **问题**:
  - `GetRecentActivities()` 返回3条写死的假活动
  - `getUserCount()` 硬编码 25/30
  - `getConnectionCount()` 硬编码 45/20/15/8/2
  - `getSessionCount()` 硬编码 12/156
  - `getTodayConnections/Sessions/NewUsers()` 全部硬编码
  - uptime 用 `time.Now().Add(-7day)` 伪造
- **修复方案**:
  - 从 SQLite 查询真实用户/连接/会话数据（模型已有）
  - 从系统日志表查询真实活动记录
  - main.go 记录启动时间，通过依赖注入传递
- **预估工时**: 1-2天
- [x] T-02 实施修复 ✅

### M-03: 性能监控假数据
- **文件**: `mini-web/backend/internal/service/system_service.go`
- **问题**:
  - `getCPUUsage()` L368: `75.5 + rand` 假数据
  - `getSystemLoad()` L419: `65.0 + rand` 假数据
  - `getNetworkStats()`: 硬编码网络连接数
  - `TestEmailConfig()` L358: 不发邮件直接返回成功
- **修复方案**:
  - CPU: 读取 `/proc/stat` 计算真实使用率
  - 负载: 读取 `/proc/loadavg`
  - 网络: 读取 `/proc/net/tcp` 统计连接数
  - 邮件: 使用 `net/smtp` 实现真实 SMTP 连接测试
- **预估工时**: 半天
- [x] T-03 CPU/负载/网络真实数据 ✅
- [x] T-04 邮件测试真实 SMTP ✅

### M-04: RDP 使用模拟实现
- **文件**: `mini-web/backend/internal/service/rdp_terminal_simple.go`
- **问题**:
  - 连接握手简化模拟 (L48, L216)
  - `sendMockDesktopData()` (L415) 发送假桌面数据
  - WebSocket 代理 `rdp_terminal_websocket_proxy.go:1168` base64 未解码直接写入
  - `rdp_terminal_real.go` 根本不存在
- **修复方案**:
  - 修复 WebSocket 代理的 base64 解码（快速）
  - 实现真实 RDP 协议（基于 grdp 或 FreeRDP CLI）
- **预估工时**: 3-5天
- [x] T-05 修复 WebSocket 代理 base64 解码 ✅
- [ ] T-06 实现真实 RDP 协议

### M-05: VNC 截图模拟
- **文件**: `mini-web/backend/internal/service/vnc_terminal.go:152`
- **问题**: 截图生成是空白模拟图
- **修复方案**: 实现真实帧缓冲区读取
- **预估工时**: 2-3天
- [x] T-07 实施修复 ✅

### M-06: API 访问统计假数据
- **文件**: `mini-web/backend/internal/service/api_control_service.go:371`
- **问题**: `GetAccessStatistics()` 返回硬编码 1234/1100/134/250
- **修复方案**: 从 SQLite 访问日志表查询真实统计
- **预估工时**: 半天
- [x] T-08 实施修复 ✅

---

## P1 — 半完成功能

### H-01: 连接服务默认 fallback 到开发测试终端
- **文件**: `mini-web/backend/internal/service/connection_service.go:378`
- **问题**: `// 默认使用本地模拟终端（仅用于开发测试）`
- **修复方案**: 完善协议路由判断，生产环境不走模拟路径
- **预估工时**: 半天
- [x] T-09 实施修复 ✅

### H-02: FileBrowser 文件编辑 UI 未集成
- **后端**: `/api/files/edit` PUT 接口已实现
- **前端**: `FileBrowser.tsx` 中无调用 `/files/edit` 的入口
- **修复方案**: 添加文件编辑入口和内联编辑器
- **预估工时**: 1天
- [x] T-10 实施修复 ✅

### H-03: 邮件功能端到端不通
- **问题**: 路由/Handler 存在，但实际不发邮件
- **依赖**: T-04
- [ ] T-11 依赖 T-04 完成后验证

---

## P2 — 技术债务

### D-01: user.go 旧 mock 登录接口
- **文件**: `mini-web/backend/internal/api/user.go:124-154`
- **问题**: `Token: "mock_token_12345"` 的假登录代码仍存在
- **预估工时**: 30分钟
- [x] T-12 清理 ✅

### D-02: 前端 LSP 错误（已存在的代码质量问题）
- **文件**:
  - `FileBrowser.tsx`: 静态元素交互事件缺失、useCallback 依赖缺失、forEach 返回值、正则控制字符
  - `FileViewer.tsx`: useEffect 依赖缺失
  - `TerminalSettings.tsx`: useEffect 依赖缺失
- **预估工时**: 1天
- [x] T-13 修复交互元素/switch/button ✅ (ESLint toolchain missing typescript-eslint, remaining are pre-existing)

---

## 推荐实施顺序

```
第1天（快速见效，2-3小时）:
  T-01 getUserIDFromContext → 1小时
  T-12 清理 mock 登录 → 30分钟
  T-05 RDP base64 解码修复 → 1小时
  T-08 API 统计接数据库 → 半天

第2-3天（核心数据真实化）:
  T-02 Dashboard 全部接 SQLite → 1-2天
  T-03 CPU/负载/网络真实数据 → 半天
  T-04 邮件真实 SMTP → 半天

第4-5天（代码质量）:
  T-09 连接服务协议路由 → 半天
  T-10 FileBrowser 文件编辑 → 1天
  T-13 前端 LSP 错误 → 1天

后续（复杂协议实现）:
  T-06 真实 RDP 协议 → 3-5天
  T-07 真实 VNC 截图 → 2-3天
```

## 总工作量估算

| 优先级 | 任务数 | 预估总工时 |
|--------|--------|------------|
| P0 违反模拟原则 | 8项 | 6-10天 |
| P1 半完成功能 | 3项 | 2-3天 |
| P2 技术债务 | 2项 | 1.5天 |
| **合计** | **13项** | **约10-15天** |
